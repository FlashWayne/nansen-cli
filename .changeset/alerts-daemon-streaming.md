---
"nansen-cli": minor
---

Add an alerts daemon that maintains an outbound WebSocket connection, letting agents react to Smart Alerts without polling or requiring an inbound port through NAT and firewalls (`nansen alerts daemon run/start/stop/status/logs`).
